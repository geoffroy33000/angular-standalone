import { EventValuePipe } from './event-value-pipe';

describe('EventValuePipe', () => {
  it('create an instance', () => {
    const pipe = new EventValuePipe();
    expect(pipe).toBeTruthy();
  });
});
